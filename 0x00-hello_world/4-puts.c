#include<stdio.h>
/**
 * main- Serves as the entry point for the program
 *
 * return-  By default returns zero if no error, otherwise returns non-zero value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
