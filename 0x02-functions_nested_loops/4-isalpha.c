#include "main.h"

/**
 * _isalpha - for lower and higher case
 *
 * @c: the int value
 *
 * Return: return 0 if successful
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	return (0);
}
