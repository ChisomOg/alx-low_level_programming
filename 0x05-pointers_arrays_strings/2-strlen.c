#include "main.h"

/**
 * _strlen - length of a string
 * @s: string
 *
 * Return: return 0 for success
 */

int _stn(char *s)
{
	int count, in;

	in = 0;
	for (count = 0; s[count] != '\0'; count++)
		in++;

	return (inc);
}
